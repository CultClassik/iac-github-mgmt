name: Terraform Configuration Run
on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - 'main'
  workflow_dispatch:

jobs:
  terraform:
    uses: Diehlabs/mywf/.github/workflows/terraform-local.yml@main
